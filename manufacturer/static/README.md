# Reserved for neutral Figma-downloaded assets (logo / chevrons).

# Phase 1 renders the logo as a text wordmark + bioicon mark inline
# (see ui/icons.py `wordmark()`) and the chevron as an inline SVG
# (ui/icons.py `BIOICONS["chevron-down"]`). No raster Figma assets are
# pulled — the Q-engine "Logo" node is a cropped raster image and the
# scientific tone rule prefers inline monochrome SVG. Add neutral assets
# here in a later phase if needed.