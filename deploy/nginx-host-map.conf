# Goes in the http{} block, not a server{} — /etc/nginx/conf.d/*.conf is
# included from http{}, so dropping this beside nginx-host.conf works.
# Defines the $connection_upgrade variable that file uses.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
